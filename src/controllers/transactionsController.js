const transactionsController = {
  getLastTen: async (req, res, next) => {
    // get user account balance
    // find last 10 transaction entries
    // return last 10 & account balance
  },

  create: async (req, res, next) => {
    // req.body => transactions.create()
  }
}

module.exports = transactionsController
